package ex_16_Arrays;

/*public class Arrays_Max_Value_interview {
   / public static void main(String[] args) {
       int[] array = {25, 14, 56, 15, 36, 56, 77, 18, 29, 49};
        // MAX, MIN

        // Logic Building
        // 1 ->  inputs - array -> int
        // output ->  int - 77
        int max_output = give_max(array);
        int min_output = give_min(array);
        System.out.println("Maximum value: " + max_output);
        System.out.println("Minimum value: " + min_output);

        public static int give_max(int[] arr){
            int max = arr[0];
            for (int num : arr) {
                if (num > max) {
                    max = num;
                }
            }

        }
        return max;
        public static int give_min(int[] arr){
            int min = arr[0];
            for (int num : arr) {
                if (num < min) {
                    min = num;
                }
            }

        }
        return min;
    }
}
*/